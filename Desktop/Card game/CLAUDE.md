# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This is a new, in-progress web-based card game project. Currently only `index.html` exists (empty). No build tooling, framework, or dependencies have been chosen yet.

## Architecture

When the project is built out, update this file with:
- The chosen tech stack (framework, build tool, test runner)
- How to build, run, and test the project
- Key architectural decisions (game state management, component structure, etc.)
